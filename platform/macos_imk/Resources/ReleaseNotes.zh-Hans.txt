修复升级或残留开发副本可能启动多个输入法服务器、导致间歇性无法输入的问题；只有系统正式安装的猫栈拼音会继续运行。
启动时会核对已安装输入源的完整标识。发现旧副本或错误注册时会修复系统映射并退出当前副本，不会自动启用输入法。
已注册但尚未启用的猫栈拼音现在会被正确识别，避免重复注册或误报修复失败。
新增 macOS Writer V1：可在偏好设置中主动使用本地 AI 进行礼貌改写和中英互译预览，结果只供复制，不会自动替换原文。
Writer 模型约 1.1 GB，仅在用户主动操作后从固定官方来源下载；下载与使用前都会校验大小和 SHA-256，推理不使用云端。
严格隐私模式会强制停用并取消 Writer；模型缺失、超时、崩溃或队列繁忙均不影响普通拼音输入和 AI Lite 候选排序。
