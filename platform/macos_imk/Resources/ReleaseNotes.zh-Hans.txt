偏好设置改为紧凑的一级总览与二级功能页。词库管理、本地 Writer、版本与更新各自独立，后续增加功能不会继续拉长主页面。
隐私、预测、用户学习、词库导入、Writer、更新检查和重新加载仍沿用原有行为，窗口也继续支持等比例缩放。
修复升级或残留开发副本可能启动多个输入法服务器、导致间歇性无法输入的问题；只有系统正式安装的猫栈拼音会继续运行。
启动时会核对已安装输入源的完整标识，正确识别已注册但尚未启用的输入源，不会自动启用输入法。
macOS Writer V1 可在偏好设置中主动使用本地 AI 进行礼貌改写和中英互译预览，结果只供复制，不会自动替换原文。
Writer 模型约 1.1 GB，仅在用户主动操作后下载并校验；严格隐私模式会强制停用 Writer，普通拼音输入和 AI Lite 候选排序不受影响。
