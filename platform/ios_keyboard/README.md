# iOS Keyboard Extension

This directory will contain the iOS container app and keyboard extension in stage 07.

Required defaults:

- `RequestsOpenAccess=false`.
- Include a Globe or Next Keyboard key.
- Keep local learning opt-in and privacy-preserving.
