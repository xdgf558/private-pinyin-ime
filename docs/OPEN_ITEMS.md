# Open Items

| ID | Stage | Item | Priority | Owner | Status | Notes |
|---|---|---|---|---|---|---|
| OI-001 | 01 | Replace starter lexicon with licensed production lexicon | High | Codex | closed | Stage 13 replaces the starter base lexicon with an owner-approved AOSP PinyinIME rawdict import supplemented by mozillazg pinyin-data single-character readings; manifest records Apache-2.0/MIT sources, exact revisions, 100,657 entries, and release approval |
| OI-002 | 00 | Select final project license | Medium | Owner | open | Stage 12 records this as a public release gate; repository currently uses all-rights-reserved text |
| OI-003 | 01 | Implement Rust core engine crate | High | Codex | closed | Completed in stage 01 local branch |
| OI-004 | 01 | Add minimal GitHub Actions for Rust validation | High | Codex | closed | Completed in stage 01 local branch |
| OI-005 | 01 | Keep runtime data outside source directories | Medium | Codex | open | Development-only sandbox data may use ignored `local/`; production code should use platform data directories |
| OI-006 | 02 | Add indexed lexicon lookup before production dictionary scale | High | Codex | closed | Stage 09 builds a compact-pinyin sorted index and uses binary prefix ranges for base lexicon lookup |
| OI-007 | 04 | Refine Shift toggle to key-up-only semantics in platform hosts | Medium | Codex | open | Stage 01 toggles on a synthetic Shift key event; real hosts should distinguish standalone Shift release from Shift+letter |
| OI-008 | 03 | Implement candidate paging for page up/down and page-size settings | Medium | Codex | closed | Stage 09 pages candidate output by `candidate_page_size`; PageUp/PageDown and ArrowUp/ArrowDown move between pages and digit selection applies to the visible page |
| OI-009 | 02 | Commit first candidate before punctuation during composition | Medium | Codex | closed | Stage 09 commits the first visible candidate plus punctuation, such as `你好,` for `nihao,`, and falls back to raw input only when no candidate exists |
| OI-010 | 02 | Use range-prefix queries for SQLite user lexicon lookup | Medium | Codex | closed | Stage 09 replaces `LIKE` with `compact_pinyin >= ? AND compact_pinyin < ?` range queries |
| OI-011 | 02 | Preserve exact user lexicon matches before applying query limits | Medium | Codex | closed | Stage 09 queries exact user pinyin rows separately before prefix-limited rows |
| OI-012 | 02 | Fuse user and base ranking instead of unconditional user-first ordering | Medium | Codex | closed | Stage 09 sorts merged candidates by exact/prefix tier, source boost, frequency, and stable text/pinyin tie-breaks before deduplication |
| OI-013 | 02 | Wire sanitized user lexicon database failures into logging | Medium | Codex | closed | Stage 09 emits structured `error code=...` log events for user lexicon lookup/learning failures without raw input, pinyin, candidate text, or context |
| OI-014 | 03 | Add C ABI settings loader for host-provided configuration | High | Codex | closed | Completed in stage 06; `ime_engine_new(config_json_path)` loads JSON settings and the C ABI exposes user lexicon clear/export actions |
| OI-015 | 04 | Add Windows code signing for TSF DLL, PowerShell installer scripts, and MSI | High | Codex | open | Stage 12 adds SignTool hooks for binaries/MSI, Authenticode hooks for staged `.ps1` scripts, and a `-RequireSigning` release-candidate gate; production still needs owner certificate and signed-artifact evidence |
| OI-016 | 04 | Build production Windows installer and uninstaller | High | Codex | open | Stage 12 extends the WiX MSI/zip packaging path with signing support; production still needs signed MSI install/uninstall validation on Windows |
| OI-017 | 04 | Polish Windows candidate window for high DPI and dark mode | Medium | Codex | closed | Stage 10 scales the non-activating candidate popup by monitor DPI, follows the Windows app light/dark preference, clamps to the work area, and keeps paging behavior in the Rust core |
| OI-018 | 04 | Add Windows CI or manual validation record for TSF DLL loading | Medium | Codex | open | macOS local validation cannot load TSF; verify Notepad smoke test on Windows 11 before treating the host as user-ready |
| OI-019 | 04 | Position Windows candidate popup with TSF text extents | Medium | Codex | closed | Stage 10 positions the candidate popup from `ITfContextView::GetTextExt` inside the edit session and keeps a caret fallback when text extents are unavailable |
| OI-020 | 04 | Unregister the Windows candidate window class on unload | Medium | Codex | closed | Stage 10 registers the candidate window class once and calls `UnregisterClassW` during `DLL_PROCESS_DETACH` |
| OI-021 | 04 | Add TSF display attributes for preedit text | Medium | Codex | open | Stage 10 did not add a full `ITfDisplayAttributeProvider`; implement and validate display attributes on Windows before release |
| OI-022 | 04 | Add Windows CI compile coverage for the TSF host | Medium | Codex | closed | Stage 08 adds a pinned `windows-2022` job that runs `cargo test --workspace`, runs `scripts/build_windows_tsf.ps1`, and verifies `PrivatePinyinTsf.dll` is produced; runtime smoke testing remains tracked separately |
| OI-023 | 04 | Clear Windows prediction candidate state on focus loss | Medium | Codex | closed | Completed in stage-04 local review fix by hiding the candidate window and resetting host active-input state in `OnSetFocus(false)` |
| OI-024 | 04 | Reset Rust session state when Windows composition ends externally | High | Codex | closed | Completed in stage-04 local review fix by calling `ime_session_reset` through `CoreBridge::reset_session()` from `OnCompositionTerminated` and `OnSetFocus(false)` |
| OI-025 | 05 | Add macOS code signing and notarization | High | Codex | closed | macOS `0.1.14` was signed with Developer ID Application/Installer, accepted by Apple notarization as submission `9a037028-fae7-46d8-b7ef-8a9801f92571`, stapled, and accepted by Gatekeeper |
| OI-026 | 05 | Build production macOS installer and uninstaller package | High | Codex | open | Signed/notarized `0.1.14` pkg is ready; production still needs clean-user install/upgrade/uninstall validation and final release uninstall guidance |
| OI-027 | 05 | Polish macOS candidate positioning and appearance | Medium | Codex | open | Stage 05 uses the system `IMKCandidates` panel with below-caret hint; verify behavior in TextEdit, Safari, Chrome, and VS Code |
| OI-028 | 05 | Add macOS settings entry and menu icon assets | Medium | Codex | closed | Stage 10 adds a macOS preferences window for privacy, prediction, and learning toggles; the macOS icon refresh adds packaged template menu/palette icons plus a color app icon |
| OI-029 | 05 | Verify IMK candidate panel number-key routing | Medium | Codex | open | The Rust core consumes digit keys for candidate selection while `IMKCandidates` also has built-in selection key handling; validate on macOS that the two paths do not double-select or conflict |
| OI-030 | 06 | Add automatic update strategy | Medium | Owner | closed | Stage 12 records a conservative initial strategy: signed MSI/zip, signed/notarized macOS pkg, and TestFlight/App Store updates first; revisit Sparkle, MSIX, or App Installer after signed release evidence exists |
| OI-031 | 06 | Validate Windows installer and settings UI on Windows 11 | High | Codex | open | macOS local validation can inspect scripts only; run `package_windows_tsf.ps1`, install/uninstall MSI, open settings, clear/export lexicon, and smoke-test Notepad on Windows |
| OI-032 | 06 | Use one packaged default settings template across hosts | Medium | Codex | closed | Stage 11 makes desktop and iOS hosts read packaged `default_settings.json`, patch only platform lexicon paths, and adds a Rust test that keeps the template aligned with `ImeSettings::default` |
| OI-033 | 06 | Harden Rust settings/export atomic file replacement on Windows | Medium | Codex | closed | Stage 11 adds a shared `AtomicFile` helper for settings JSON and user lexicon TSV exports, using same-directory temp files plus replace/backup cleanup instead of remove+rename |
| OI-034 | 06 | Implement or hide CapsLock toggle in platform settings | Medium | Codex | closed | Stage 11 keeps CapsLock hidden from Windows, macOS, and iOS settings UI and adds source checks so it is not exposed before host semantics are implemented |
| OI-035 | 07 | Configure iOS App Store signing and provisioning | High | Owner | open | Stage 16 adds TestFlight upload ExportOptions, App Store Connect API key gating, package summaries, upload records, and source gates; release still needs real Apple team profiles, App Store metadata, signed archive/export evidence, uploaded build number, processing status, and TestFlight distribution evidence |
| OI-036 | 07 | Design App Group storage and explicit learning opt-in for iOS | High | Codex | closed | Stage 11 adds source App Group entitlements, shared settings/user-lexicon paths, and a container-app learning toggle that defaults off while `RequestsOpenAccess=false` remains unchanged |
| OI-037 | 07 | Write user-facing iOS keyboard permission explanation | Medium | Owner | closed | Stage 11 updates the iOS container app to explain Full Access, no-network behavior, local App Group storage, learning opt-in, and local lexicon clearing |
| OI-038 | 07 | Run iOS simulator smoke tests in Notes, Safari, and password fields | High | Codex | open | Stage 15 adds automated smoke-readiness checks and a record file for build artifacts, Info.plist expansion, App Group ID, `RequestsOpenAccess=false`, bundled defaults, and no-network source usage. Manual validation still must verify keyboard enablement, `nihao -> 你好`, `jintian -> 今天` prediction retention, learning opt-in, App Group storage availability, Globe switching, no Full Access, and password/phone-field system-keyboard fallback on simulator/device. If shared storage is denied, expected behavior is fallback to built-in defaults with normal typing preserved and learning disabled; product must later decide whether iOS learning requires Full Access. If prediction candidates disappear, reset the self-text-operation flag from `textDidChange` instead of synchronous `defer` |
| OI-039 | 07 | Derive iOS mode UI from C ABI output mode | Medium | Codex | closed | Stage 11 exposes `ImeOutput.mode` through the iOS Swift bridge and derives the keyboard mode label from engine output instead of local toggling |
| OI-040 | 07 | Respect `needsInputModeSwitchKey` for the iOS Globe key | Medium | Codex | closed | Stage 11 hides the Globe key when `needsInputModeSwitchKey` is false and rebuilds the keyboard row if the system requirement changes |
| OI-041 | 09 | Expose sanitized core logging through host ABI callbacks | Medium | Codex | open | Stage 09 adds core `set_log_sink` support, but Windows/macOS/iOS cannot connect it yet; add an `ime_set_log_callback`-style ABI with explicit callback lifetime and thread-safety contract before release diagnostics |
| OI-042 | 13 | Measure production lexicon engine initialization latency | Medium | Codex | open | Stage 13 embeds a 100,657-entry base lexicon with `include_str!`; measure first activation/engine creation on macOS, Windows TSF per-process activation, and iOS engine reload latency after changing inline prediction or learning settings before deciding whether binary precompiled lexicon data, shared caching, lazy loading, or a runtime settings API is needed |
| OI-043 | 13 | Replace starter bigram prediction data with licensed production source | Medium | TBD | open | Stage 13 upgrades the base lexicon, but `bigram.tsv` remains a 20-entry first-party starter set; find or generate an owner-approved prediction/bigram source, record its license/version, and update manifest/notices before release approval |
| OI-044 | User learning | Add retention policy for user learning rows | Low | Codex | open | Local learning lets `user_phrases`, `user_bigrams`, and `user_short_phrases` grow without an upper bound; define a max-row or low-frequency/LRU pruning policy before broad public release or after real-world size measurements |
| OI-045 | Core input | Add incremental lattice caching and mixed full-pinyin/initial decoding | Medium | Codex | open | The second-generation decoder jointly scores complete pinyin word paths and keeps each lookup under the current 60-ms regression budget, but still recomputes the bounded lattice on every key and treats full-pinyin and initials as separate candidate modes; add prefix-state reuse and mixed forms such as `wojt` after real-device profiling |
