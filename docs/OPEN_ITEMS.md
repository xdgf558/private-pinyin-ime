# Open Items

| ID | Stage | Item | Priority | Owner | Status | Notes |
|---|---|---|---|---|---|---|
| OI-001 | 01 | Replace starter lexicon with licensed production lexicon | High | Codex | closed | The bundled base combines owner-approved AOSP PinyinIME rawdict, mozillazg pinyin-data, and mozillazg phrase-pinyin-data; manifest records Apache-2.0/MIT sources, exact revisions, 137,699 entries, and release approval. GPL Rime dictionaries remain user-supplied local imports only |
| OI-002 | 00 | Select final project license | Medium | Owner | open | Stage 12 records this as a public release gate; repository currently uses all-rights-reserved text |
| OI-003 | 01 | Implement Rust core engine crate | High | Codex | closed | Completed in stage 01 local branch |
| OI-004 | 01 | Add minimal GitHub Actions for Rust validation | High | Codex | closed | Completed in stage 01 local branch |
| OI-005 | 01 | Keep runtime data outside source directories | Medium | Codex | open | Development-only sandbox data may use ignored `local/`; production code should use platform data directories |
| OI-006 | 02 | Add indexed lexicon lookup before production dictionary scale | High | Codex | closed | Stage 09 builds a compact-pinyin sorted index and uses binary prefix ranges for base lexicon lookup |
| OI-007 | 04 | Refine Shift toggle to key-up-only semantics in platform hosts | Medium | Codex | open | Stage 01 toggles on a synthetic Shift key event; real hosts should distinguish standalone Shift release from Shift+letter |
| OI-008 | 03 | Implement candidate paging for page up/down and page-size settings | Medium | Codex | closed | Stage 09 pages candidate output by `candidate_page_size`; PageUp/PageDown and ArrowUp/ArrowDown move between pages and digit selection applies to the visible page |
| OI-009 | 02 | Commit first candidate before punctuation during composition | Medium | Codex | closed | Stage 09 commits the first visible candidate plus punctuation, such as `你好,` for `nihao,`, and falls back to raw input only when no candidate exists |
| OI-010 | 02 | Use range-prefix queries for SQLite user lexicon lookup | Medium | Codex | closed | Stage 09 replaces `LIKE` with `compact_pinyin >= ? AND compact_pinyin < ?` range queries |
| OI-011 | 02 | Preserve exact user lexicon matches before applying query limits | Medium | Codex | closed | Stage 09 queries exact user pinyin rows separately before prefix-limited rows |
| OI-012 | 02 | Fuse user and base ranking instead of unconditional user-first ordering | Medium | Codex | closed | Stage 09 sorts merged candidates by exact/prefix tier, source boost, frequency, and stable text/pinyin tie-breaks before deduplication |
| OI-013 | 02 | Wire sanitized user lexicon database failures into logging | Medium | Codex | closed | Stage 09 emits structured `error code=...` log events for user lexicon lookup/learning failures without raw input, pinyin, candidate text, or context |
| OI-014 | 03 | Add C ABI settings loader for host-provided configuration | High | Codex | closed | Completed in stage 06; `ime_engine_new(config_json_path)` loads JSON settings and the C ABI exposes user lexicon clear/export actions |
| OI-015 | 04 | Add Windows code signing for TSF DLL, PowerShell installer scripts, and MSI | High | Codex | open | Stage 12 adds SignTool hooks for binaries/MSI, Authenticode hooks for staged `.ps1` scripts, and a `-RequireSigning` release-candidate gate; production still needs owner certificate and signed-artifact evidence |
| OI-016 | 04 | Build production Windows installer and uninstaller | High | Codex | open | Stage 12 extends the WiX MSI/zip packaging path with signing support; production still needs signed MSI install/uninstall validation on Windows |
| OI-017 | 04 | Polish Windows candidate window for high DPI and dark mode | Medium | Codex | closed | Stage 10 scales the non-activating candidate popup by monitor DPI, follows the Windows app light/dark preference, clamps to the work area, and keeps paging behavior in the Rust core |
| OI-018 | 04 | Add Windows CI or manual validation record for TSF DLL loading | Medium | Codex | open | macOS local validation cannot load TSF; verify Notepad smoke test on Windows 11 before treating the host as user-ready |
| OI-019 | 04 | Position Windows candidate popup with TSF text extents | Medium | Codex | closed | Stage 10 positions the candidate popup from `ITfContextView::GetTextExt` inside the edit session and keeps a caret fallback when text extents are unavailable |
| OI-020 | 04 | Unregister the Windows candidate window class on unload | Medium | Codex | closed | Stage 10 registers the candidate window class once and calls `UnregisterClassW` during `DLL_PROCESS_DETACH` |
| OI-021 | 04 | Add TSF display attributes for preedit text | Medium | Codex | open | Stage 10 did not add a full `ITfDisplayAttributeProvider`; implement and validate display attributes on Windows before release |
| OI-022 | 04 | Add Windows CI compile coverage for the TSF host | Medium | Codex | closed | Stage 08 adds a pinned `windows-2022` job that runs `cargo test --workspace`, runs `scripts/build_windows_tsf.ps1`, and verifies `PrivatePinyinTsf.dll` is produced; runtime smoke testing remains tracked separately |
| OI-023 | 04 | Clear Windows prediction candidate state on focus loss | Medium | Codex | closed | Completed in stage-04 local review fix by hiding the candidate window and resetting host active-input state in `OnSetFocus(false)` |
| OI-024 | 04 | Reset Rust session state when Windows composition ends externally | High | Codex | closed | Completed in stage-04 local review fix by calling `ime_session_reset` through `CoreBridge::reset_session()` from `OnCompositionTerminated` and `OnSetFocus(false)` |
| OI-025 | 05 | Add macOS code signing and notarization | High | Codex | closed | macOS `0.1.17` was signed with Developer ID Application/Installer, accepted by Apple notarization as submission `90edbce9-e28f-40a9-9f98-71830dad8839`, stapled, and accepted by Gatekeeper |
| OI-026 | 05 | Build production macOS installer and uninstaller package | High | Codex | open | Signed/notarized `0.1.17` pkg is ready; production still needs clean-user install/uninstall validation, four-host horizontal-candidate smoke, and final release uninstall guidance |
| OI-027 | 05 | Polish macOS candidate positioning and appearance | Medium | Codex | open | macOS `0.1.17` uses the native horizontal 9-column stepping panel with below-caret hint; verify width, placement, stepping, and number selection in TextEdit, Safari, Chrome, and VS Code |
| OI-028 | 05 | Add macOS settings entry and menu icon assets | Medium | Codex | closed | Stage 10 adds a macOS preferences window for privacy, prediction, and learning toggles; the macOS icon refresh adds packaged template menu/palette icons plus a color app icon |
| OI-029 | 05 | Verify IMK candidate panel number-key routing | Medium | Codex | open | macOS `0.1.17` sets `IMKCandidatesSendServerKeyEventFirst=true`, so the Rust core consumes `1` through `9` before the native panel can process them; close after confirming each key selects exactly once in TextEdit, Safari, Chrome, and VS Code |
| OI-030 | 06 | Add automatic update strategy | Medium | Owner | closed | Stage 12 records a conservative initial strategy: signed MSI/zip, signed/notarized macOS pkg, and TestFlight/App Store updates first; revisit Sparkle, MSIX, or App Installer after signed release evidence exists |
| OI-031 | 06 | Validate Windows installer and settings UI on Windows 11 | High | Codex | open | macOS local validation can inspect scripts only; run `package_windows_tsf.ps1`, install/uninstall MSI, open settings, clear/export lexicon, and smoke-test Notepad on Windows |
| OI-032 | 06 | Use one packaged default settings template across hosts | Medium | Codex | closed | Stage 11 makes desktop and iOS hosts read packaged `default_settings.json`, patch only platform lexicon paths, and adds a Rust test that keeps the template aligned with `ImeSettings::default` |
| OI-033 | 06 | Harden Rust settings/export atomic file replacement on Windows | Medium | Codex | closed | Stage 11 adds a shared `AtomicFile` helper for settings JSON and user lexicon TSV exports, using same-directory temp files plus replace/backup cleanup instead of remove+rename |
| OI-034 | 06 | Implement or hide CapsLock toggle in platform settings | Medium | Codex | closed | Stage 11 keeps CapsLock hidden from Windows, macOS, and iOS settings UI and adds source checks so it is not exposed before host semantics are implemented |
| OI-035 | 07 | Configure iOS App Store signing and provisioning | High | Owner | open | Stage 16 adds TestFlight upload ExportOptions, App Store Connect API key gating, package summaries, upload records, and source gates; release still needs real Apple team profiles, App Store metadata, signed archive/export evidence, uploaded build number, processing status, and TestFlight distribution evidence |
| OI-036 | 07 | Design App Group storage and explicit learning opt-in for iOS | High | Codex | closed | Stage 11 adds source App Group entitlements, shared settings/user-lexicon paths, and a container-app learning toggle that defaults off while `RequestsOpenAccess=false` remains unchanged |
| OI-037 | 07 | Write user-facing iOS keyboard permission explanation | Medium | Owner | closed | Stage 11 updates the iOS container app to explain Full Access, no-network behavior, local App Group storage, learning opt-in, and local lexicon clearing |
| OI-038 | 07 | Run iOS simulator smoke tests in Notes, Safari, and password fields | High | Codex | open | Stage 15 adds automated smoke-readiness checks and a record file for build artifacts, Info.plist expansion, App Group ID, `RequestsOpenAccess=false`, bundled defaults, and no-network source usage. The 2026-07-18 regression pass verifies Xcode 27 build/install, extension-local nine-key/script persistence across a full simulator restart, focused `64426 -> 你好` core behavior, and `zyao -> 主要`; delayed self-generated callbacks now require matching document identity and captured context within 250 ms, while shared/local preference conflicts resolve by their write timestamps. Final device validation still must cover candidate/symbol taps, revised nine-key geometry, prediction retention, learning opt-in, container-side local Rime document-picker import/clear into the App Group, App Group availability, Globe switching, no Full Access, password/phone fallback, rapid field switching, and real host-app latency. If shared storage is denied, typing and local layout/script preferences must continue through the extension sandbox while importing and shared learning fail closed |
| OI-039 | 07 | Derive iOS mode UI from C ABI output mode | Medium | Codex | closed | Stage 11 exposes `ImeOutput.mode` through the iOS Swift bridge and derives the keyboard mode label from engine output instead of local toggling |
| OI-040 | 07 | Respect `needsInputModeSwitchKey` for the iOS Globe key | Medium | Codex | closed | Stage 11 hides the Globe key when `needsInputModeSwitchKey` is false and rebuilds the keyboard row if the system requirement changes |
| OI-041 | 09 | Expose sanitized core logging through host ABI callbacks | Medium | Codex | open | Stage 09 adds core `set_log_sink` support, but Windows/macOS/iOS cannot connect it yet; add an `ime_set_log_callback`-style ABI with explicit callback lifetime and thread-safety contract before release diagnostics |
| OI-042 | 13 | Measure production lexicon engine initialization latency | Medium | Codex | open | The reviewed permissive base now embeds 137,699 entries with `include_str!`, and a new engine snapshot may additionally parse a bounded imported layer. macOS now shares one parsed snapshot across all per-application IMK controllers and has an executable 24-session regression; complete the 5/20-client Activity Monitor smoke plus Windows TSF per-process activation and iOS engine reload/import measurements before deciding whether binary precompiled lexicon data or further lazy loading is needed |
| OI-043 | 13 | Replace starter bigram prediction data with licensed production source | Medium | TBD | open | Stage 13 upgrades the base lexicon, but `bigram.tsv` remains a 20-entry first-party starter set; find or generate an owner-approved prediction/bigram source, record its license/version, and update manifest/notices before release approval |
| OI-044 | User learning | Add retention policy for user learning rows | Low | Codex | closed | Local learning now caps `user_phrases` and `user_bigrams` at 20,000 rows, `user_short_phrases` at 10,000, and `user_trigrams` at 20,000; when a table crosses its limit, the lowest 30-day-half-life decayed weights and oldest ties are evicted first |
| OI-045 | Core input | Add incremental lattice caching and mixed full-pinyin/initial decoding | Medium | Codex | closed | The shared decoder now retains a session-local bounded lattice, extends only newly exposed suffix positions, truncates safely for backspace, invalidates reuse when apostrophe boundaries or context change, and jointly scores full-pinyin plus exact-initial edges; `wojt` ranks `我今天` first through both Rust and C ABI tests while `abc` keeps its raw-input fallback and mixed lookup remains under the 60-ms regression budget |
| OI-046 | 17 | Deduplicate visually identical iOS candidates after script conversion | Low | Codex | open | Distinct normalized Simplified candidates can occasionally render as the same Traditional text; add a display-layer deduplication map that preserves the correct core candidate index and learning identity before treating Traditional output as polished |
| OI-047 | Local lexicon | Make multi-file imports transactional and serialize writers | Low | Codex | open | Current hosts import selected Rime files sequentially and report partial success; each file replacement is atomic, but a later failure does not roll back earlier files and concurrent processes can overwrite one another's newly merged rows. Add a parse-all batch API plus an interprocess writer lock before treating bulk import as fully transactional |
| OI-048 | Local lexicon | Unify imported-lexicon manifest handling across hosts and CLI | Low | Codex | open | macOS and iOS currently maintain parallel manifest implementations, while settings CLI imports do not update the UI manifest and therefore fall back to an unavailable-source status. Define one shared schema/helper boundary and make every supported import entry point update status consistently without storing source paths or dictionary content |
| AI-OI-001 | AI-01/08 | Capture Windows, Intel macOS, and real-device iOS latency and resident-memory baselines | Medium | Codex | open | AI-08 has deterministic fallback coverage, an iOS 27 simulator build, and controller-lifetime AI suspension after memory warnings. Real-device first-enable latency, extension RSS, available-memory rejection/recovery, and memory-pressure behavior remain required before calibrating the approved AI Lite budget; test at least one 8-GiB device and one sub-8-GiB device |
| AI-OI-002 | AI-06 | Select the compact AI Lite training method and approved training sources | High | Owner/Codex | closed | AI-06 selects a shared fixed-point Rust linear ranker and a first-party hand-calibrated 426-byte coefficient package derived only from repository regressions and synthetic cases; ranker `ai06-v1`, feature schema `1`, exact artifact fingerprint `8bc7977a88f64a818fd232b7cfafd19af477232259e700d690ea37dfa639d439`, license notice, local-only privacy declaration, 8/8 targeted improvements, 4/4 preservation gates, and Owner registry approval are tracked in the reviewed package |
| AI-OI-003 | AI-07/08 | Define secure-input and composition-revision signals across platform hosts | High | Codex | closed | Every local-AI session has opaque identity plus monotonic revision/request identity; macOS forwards `IsSecureEventInputEnabled`, Windows classifies TSF password input scope and fails closed on probe failure, and AI-08 relies on iOS secure-field keyboard replacement while additionally failing closed for numeric/phone traits. Secure or stale work is cancelled and discarded before it can affect candidates; real-device field behavior remains a release smoke item rather than a contract gap |
| AI-OI-004 | AI-10/11 | Select an optional redistributable desktop Writer model | High | Owner | open | AI-10 pinned Qwen2.5 0.5B Instruct Q4_K_M revision `9217f5db79a29953eb74d5343926648285ec7e67` with llama.cpp `b10069`: about 502 MiB model/runtime download, 579 MiB measured peak RSS, 276-295 ms first byte, cancellation within budget, but only 2/3 Chinese quality cases passed. Decision 040 records No-Go; evaluate a stronger exact candidate and obtain separate Owner/license redistribution approval before AI-11 |
| AI-OI-005 | AI-07/08 | Enforce bounded worker-queue dispatch for synchronous local AI providers | High | Codex | closed | Desktop and isolated iOS FFI builds use the same bounded non-blocking local worker; IMK, TSF, and keyboard callbacks only submit/poll, queue saturation preserves base candidates, cancellation removes pending output, and completion is accepted only when session, request, revision, candidate hash, deadline, secure state, and exact candidate text still match |
| AI-OI-006 | AI-12 | Build a categorized privacy regression corpus and tune false positives | Medium | Codex | open | Move privacy cases into maintained password, token, identity-card, phone, payment, and false-positive groups; preserve assignment-aware handling for ordinary phrases such as `token economy`, `secret garden`, and `password manager`, then evaluate bounded context rules, explicit confirmation, and narrow allowlists before enabling broader AI features |
| AI-OI-007 | AI-07 | Integrate AI-04 rules with revisions, user terms, and confirmed cleanup actions | High | Codex | open | AI-07 integrates only the approved AI-06 numeric ranker on the bounded desktop worker; typo/English-term feature slots remain zero and cleanup remains offline-only. A later reviewed stage must merge user English terms ahead of built-ins, define exact/probable/weak confidence display policy, map cleanup reason codes to localized explanations, and require explicit confirmation plus an undo snapshot before deleting any suggestion |
| AI-OI-008 | AI-07/08 | Detect and calibrate real platform hardware profiles | Medium | Codex | open | AI-07 supplies desktop physical memory; AI-08 supplies iOS physical memory plus a process-available-memory precheck and preserves base input below the approved 8-GiB manifest requirement. GPU remains conservatively unavailable. Real iOS and desktop RSS/startup measurements must determine whether the Lite threshold should be lowered and how hosts should expose unavailable AI; changing the threshold requires a new reviewed package policy/fingerprint |
| AI-OI-009 | AI-07 | Expand AI Lite quality benchmarks without production typing data | Medium | Owner/Codex | open | Desktop integration uses the reviewed AI-06 synthetic/project regression gate, but broader owner-approved public or manually authored cases for common typo patterns, mixed English terms, and longer candidate lists are still required; preserve explicit provenance and never collect, retain, anonymize, or replay user input |
| AI-OI-010 | AI-09/11 | Validate signed Helper identity and persistent-client failure handling on real desktops | High | Codex/Owner | open | AI-09 packages the helper and exercises authentication, health, cancellation, crash/restart, shutdown, and idle exit in automated lifecycle probes. Before Writer payloads are enabled, verify the separately signed helper inside a notarized macOS pkg and signed Windows installer; replace the probe-only `PIPE_NOWAIT` polling with deadline-aware blocking or overlapped I/O; test near-64-KiB writes and a live-but-silent helper against an absolute per-request deadline; add bounded Swift helper restart cooldown/budgets plus fault injection; and confirm crash, hang, queue saturation, log inspection, and ten-minute idle exit never affect ordinary typing |
| UPDATE-OI-001 | UPDATE-01 | Publish and smoke-test the live macOS stable manifest | High | Owner | open | Upload the immutable signed/notarized pkg and release page first, publish `https://wwwstationcat.org/updates/private-pinyin/macos/stable.json` last, then verify live JSON, byte size, SHA-256, current-version, newer-version, and withdrawal behavior |
| UPDATE-OI-002 | UPDATE-02 | Add verified macOS package download and Installer handoff | High | Codex/Owner | closed | The host now downloads only after explicit consent, enforces same-host HTTPS plus exact size/SHA-256, pins Team `Y35K7AQ974`, requires trusted Developer ID Installer and Gatekeeper notarization, re-verifies before opening Apple's visible Installer, and never invokes a privileged installer itself; live website smoke remains UPDATE-OI-001 |
| UPDATE-OI-003 | UPDATE-03 | Add post-update input-method process refresh guidance | Medium | Codex | closed | The pkg now launches its signed executable as a dedicated UI-only process in the console user's Aqua session; it uses a strict subsecond handoff cutoff so same-boundary and later same-bundle processes are preserved, revalidates captured PIDs after user consent, requests normal exit without force-kill, reports success without logout/restart, and falls back to logout/login guidance only when an old process remains |
